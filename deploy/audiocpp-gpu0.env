AUDIO_UI_FLAG=--no-ui
# GPU1 nunca serve UI -- e alvo do pool balanceado :8080; o upload_root_
# do audiocpp e por processo, entao um upload feito aqui nao existiria na
# outra placa. Alternado por: v100ctl audio start [--web] (so este arquivo).
